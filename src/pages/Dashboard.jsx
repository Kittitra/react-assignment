

function Dashboard() {
  // const { user, logout } = useContext(AuthContext);

  // if (!user) {
  //   return <h2>กรุณาเข้าสู่ระบบ</h2>;
  // }

  return (
    <div>
      <h2>Dashboard</h2>
      
    </div>
  );
}

export default Dashboard;